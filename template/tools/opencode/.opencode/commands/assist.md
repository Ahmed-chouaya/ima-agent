---
description: Start a guided IMA workflow — asks questions, builds a pipeline, walks you through it
agent: ima-assist
---

Read `.ima/agents/assist.md` and start the guided workflow. Check for `progress.yaml` to resume. Execute: `$1`

If `$1` is empty, begin the full intake flow.
